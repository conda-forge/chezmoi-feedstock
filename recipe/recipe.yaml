schema_version: 1

context:
  version: "2.70.4"

package:
  name: chezmoi
  version: ${{ version }}

source:
  url: https://github.com/twpayne/chezmoi/archive/refs/tags/v${{ version }}.tar.gz
  sha256: a184d87b2891e8ec5645d4a28a0e2f78c1a4e7738ad64fdfe07e8f7ce7782d59

build:
  number: 0

requirements:
  build:
    - ${{ compiler('go-nocgo') }}
    - go-licenses

tests:
  - script:
      content:
        # workaround https://github.com/prefix-dev/rattler-build/issues/2424
        - if: osx and x86_64
          then:
            - export HOME=${HOME:-$PWD}
        - chezmoi --help

about:
  license: MIT
  license_file:
    - LICENSE
    - license-files/
  summary: Manage your dotfiles across multiple diverse machines, securely
  homepage: https://chezmoi.io
  repository: https://github.com/twpayne/chezmoi

extra:
  recipe-maintainers:
    - anjos
    - danielnachun
